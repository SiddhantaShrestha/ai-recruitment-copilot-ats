export async function triggerApplicationWorkflow(
  applicationId: string
): Promise<void> {
  // Placeholder for future workflow integration (email, screening pipeline, etc.)
  console.log("Trigger workflow for application:", applicationId);
}